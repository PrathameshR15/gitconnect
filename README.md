# Git Connect Calculator

A simple command-line calculator written in Python that supports basic arithmetic operations:
- Addition (`+`)
- Subtraction (`-`)
- Multiplication (`*`)
- Division (`/`)

## How to Run

Ensure you have Python installed, then run:

```bash
python calculator.py
```